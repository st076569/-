First time : это время работы первого алгоритма (сек).
for (int i = 0; i <= k; i++) {
     s+=a[i];
}
Second time : это время работы второго алгоритма (сек).
for (short int* p = a; p != q; p++) {
     s+=*p;
}
На картинке представлены результаты обработки массива из 5е+7 элементов (6 тестов).
Видно, что алгоритм обработки №2 быстрее.